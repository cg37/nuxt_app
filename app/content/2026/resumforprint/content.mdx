export const metadata = {
    title: '个人简历打印版',
    titleEng: 'resumeForPrint',
    hideTitle: true,
}
import Table from '/components/Table.vue'

## 联系方式

手机: 15958040921

Email: CG37@outlook.com

WeChat: CraigC37

## 个人信息

陈港/男/1997

本科/测控技术与仪器/CET 6

浙江工业大学 2015-2019

## 技能清单

- 精通 `HTML5/CSS3`，熟练实现响应式布局、`Flex/Grid` 自适应及动画效果
- 熟练掌握 `JavaScript` 基础，深入理解 `ES6+` 特性、闭包、原型链及异步编程
- 熟练使用 `Vue 3` 全家桶(`Pinia/Vue Router`)，理解响应式原理与组合式 `API` 设计模式
- 熟悉 `React` 生态(`Hooks/Redux`)及 `React Native` 跨端开发
- 熟练使用 `Vite` 进行工程化构建与性能优化
- 有 `NPM` 包及 `VS Code` 插件的独立开发、发布与维护经验
- 了解 `C#/.NET` 后端开发，具备全栈协作能力与接口联调经验
- 熟练使用 `Git` 进行版本控制，掌握 `Git Flow` 工作流；熟练搭建 `GitHub Actions` 的 `CI/CD` 流水线
- 掌握 `Spec Coding` 规范驱动开发，熟练运用主流 `AI Agent` 工具提升编码效率

## 工作经历

<Table />

## 项目经验

### 美团 闪购技术部 牵牛花PMS平台

牵牛花平台是美团闪购业务的核心运营管理系统，支撑商家运营、商品管理及数据分析等核心场景

技术栈: `React` + `Vite` + `TypeScript`

#### 工作内容

- 设计 `React` 工厂模式列表页组件库，封装筛选、表格、分页等通用能力；通过 `Provider` + 多层级 `Context` 管理状态，支持 URL 双向同步及多 Tab 状态隔离
- 基于公司内部基础库 `@roo/roo` 封装业务组件库(含供应商选择、门店选择、商品信息展示等组件)，统一交互体验
- 实现租户级差异化渲染，满足多租户、灰度发布等复杂业态下的定制化展示需求

### 美团 闪购技术部 MRN 移动端工程

牵牛花 APP 是基于美团 `MRN(React Native)` 平台的移动端采购执行工具，覆盖采购全链路业务场景，采用多 `Bundle` 架构实现各业务模块独立打包发布

技术栈: `React Native` + `TypeScript` + `@mrn/react-navigation` + `jotai` + `ahooks`

#### 工作内容

- 采购域 4 个核心 `Bundle` 的融合重构，完成从 0 到 1 的目录结构搭建，包含路由配置、服务层封装、状态管理及页面组件拆分，确保各 `Bundle` 独立性
- 基于公司内部基础库 `@sfe/wand-rn` 和 `@sgfe/flower-rn` 封装通用布局组件，通过 `HOC` 统一处理埋点、权限校验、业务配置等横切关注点
- 拦截不安全数值运算，建立 `ESLint` 规则 + `MR` 模板 + `CI` 卡口三层防护机制，从流程上杜绝浮点精度隐患

### 华为云 EI产品服务部 数据资产平台

作为华为云大模型数据支撑平台，为百亿参数训练提供多模态(文本/视频/结构化数据)的资产管理

技术栈: `Vue 3` + `Vue Router` + `Vite` + `TypeScript` + `ECharts 5`

#### 工作内容

- 开发支持 PB 级文本/视频/多模态数据的预览系统，采用虚拟滚动/分片加载等技术实现视频智能分段、文本动态分页；实现资产数据多维度检索
- 基于 `ECharts 5` 开发数据资产可视化看板，扩展封装图表组件
- 搭建数据处理任务管线，实现资产平台的前端导入、导出、更新功能，开发任务处理进度可视化及详情展示组件
- 前端工程化实践：`Vite` 打包压缩减小产物体积，提升流水线构建速度；建立 `ESLint` + `Prettier` + `Husky` 全链路代码规范，集成至 `CI/CD` 流水线实现自动化部署
- 探索 `Tauri 2.0` 在项目中的落地，安装包体积较 `Electron` 缩减 **65%**

### 华为云 EI产品服务部 具身仿真平台

面向具身智能场景的机械臂仿真与控制系统，支持脚本编排、任务执行、实时状态查看及高精度标定

技术栈: `Vue 3` + `NoVNC` + `WebSocket`

#### 工作内容

- 从零搭建标准化前端脚手架，统一依赖与构建配置
- 搭建服务端至前端的 `VNC` 实时传输管道，优化 `NoVNC` 降低带宽占用(70%)
- 封装脚本编辑器、状态监控面板等高复用业务组件，统一交互规范，显著提升团队开发效率与代码复用率

## 个人项目

### nuxt-mdx（个人文档站点）

基于 `Vue 3` + `Nuxt` 的个人技术博客，使用 `Composition API` 封装可复用逻辑，
通过自定义组件实现 `MDX` 内容渲染。
样式层基于 `SCSS` 预处理构建设计系统，自动生成亮/暗双主题，
统一设计令牌管理确保视觉一致性，并适配响应式排版与 `@media print` 打印样式。
工程化采用 `Vite` 构建 + `TypeScript` + `ESLint`/`Prettier`

- [GitHub 项目地址](https://github.com/cg37/nuxt_pdf)

### PlayWrightPdfService(端到端 PDF 生成基础设施)

一套包含后端服务、前端 SDK、安全认证、容器化部署的 PDF 生成技术方案。
后端使用 `.NET 10` + `Playwright` 渲染引擎，
生产部署上，服务运行在 `Docker` 容器中，
通过 `Cloudflare Zero Trust` (Access + Tunnel) 实现零暴露面的公网访问，
同时内置 `API Key` 第二层认证兜底

- [GitHub 项目地址](https://github.com/cg37/PlayWrightPdfService)

### natural2bash (自然语言转 Bash 命令)

一个基于 AI 的命令行工具，将中文/英文自然语言描述自动转换为可执行的 `Bash` 命令。
技术栈使用 `Node.js` 构建 `CLI` 框架，`Chalk` + `Ora` 优化终端视觉效果。
内置 `GitHub Actions` 自动发布流水线：推送代码触发多版本 `Node.js` (18.x / 20.x / 22.x)
测试的 `CI` 流水线，发布 `GitHub Release` 后自动触发 `npm` 公共仓库发布的 `CD` 流水线。

- [项目地址](https://github.com/cg37/natural2bash)

### FluxChat

基于 `Vue 3` 的 `ChatGPT` 客户端。核心基于 `SSE` 协议实现流式输出，
通过 `@microsoft/fetch-event-source` 逐 `token` 解析 `OpenAI` 数据流，实时增量渲染至 `UI`，达到打字机般逐字呈现效果。
同时实现了自动滚动与用户交互暂停的智能联动：`AI` 输出期间话题区自动跟随滚动；用户触摸或滚轮操作后，
自动滚动暂停，避免打断阅读，交互恢复后重新跟随，整体提升了长文本对话的阅读连贯性和操作可控性。

- [项目地址](https://github.com/cg37/FluxChat)

## 个人总结

5年+ 前端开发经验，深度参与B端管理、数据可视化大屏、移动端跨端的核心模块研发与重构

独立开发并发布了涵盖 `NPM` 包、`VS Code` 插件、`CLI` 工具及桌面端 (`Tauri`/`Swift`) 的提效产品

熟练搭建 `CI/CD` 自动化流水线，主导过前端代码规范门禁、通用组件库封装及构建体积优化，提升团队研发效能

熟练阅读英文一手技术文档；积极拥抱 `AI Agent` 提效与 `Spec Coding` 规范，具备技术落地与独立解决问题的能力
