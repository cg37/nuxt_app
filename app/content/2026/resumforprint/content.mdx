export const metadata = {
    title: '个人简历打印版',
    titleEng: 'resumeForPrint',
    hideTitle: true,
}
import Table from '/components/Table.vue'

## 联系方式

手机: 15958040921

Email: CG37@outlook.com

WeChat: CraigC37

## 个人信息

陈港/男/1997

本科/CET-6/浙江工业大学

测控技术与仪器 2015-2019

## 技能清单

- 精通 `HTML5/CSS3`，熟练实现响应式布局、`Flex/Grid` 自适应及动画效果
- 扎实的 `JavaScript` 基础，深入理解 `ES6+` 特性、闭包、原型链及异步编程
- 熟练使用 `Vue3` 全家桶(`Pinia/Vue Router`)，理解响应式原理与组合式 `API` 设计模式
- 熟悉 `React` 生态(`Hooks/Redux`)及 `React Native` 跨端开发，具备组件化架构能力
- 具备 `Nuxt.js` `SSR` 服务端渲染实战经验，熟练使用 `Vite` 进行工程化构建与性能优化
- 拥有 `NPM` 包及 `VS Code` 插件的独立开发、发布与维护经验
- 熟练搭建基于`GitHub Actions`的`CI/CD`流水线，实现代码扫描、自动化构建与容器化部署
- 掌握计算机网络基础，熟悉 `HTTP/HTTPS`、`TCP/IP`、`WebSocket` 等协议
- 了解 `C#/.NET` 后端开发，具备全栈协作能力与接口联调经验
- 熟练使用 `Git` 进行版本控制，掌握 `Git Flow` 工作流与团队协作规范
- 掌握 `Spec Coding` 规范驱动开发，熟练运用主流 `AI Agent` 工具提升编码效率

## 工作经历

<Table />

## 项目经验

#### 美团 闪购技术部 牵牛花PMS平台

##### 项目定位

牵牛花平台是美团闪购业务的核心运营管理系统，支撑商家运营、商品管理及数据分析等核心场景。
技术栈：`React` + `Vite` + `TypeScript` + `Lodash`

##### 核心工作

1. 设计 React 工厂模式列表页组件库，封装筛选、表格、分页等通用能力；通过 `Provider` + 多层级 `Context` 管理状态，支持 URL 双向同步及多 Tab 状态隔离
2. 基于公司内部基础库 @roo/roo 封装业务组件库（含供应商选择、门店选择、商品信息展示等 10+ 组件），统一 5+ 业务域的交互体验
3. 实现租户级差异化渲染，满足多租户、灰度发布等复杂业态下的定制化展示需求

#### 美团 闪购技术部 MRN 移动端工程

##### 项目描述

牵牛花 APP 是基于美团 `MRN(React Native)`平台的移动端采购执行工具，覆盖采购全链路业务场景，采用多 `Bundle` 架构实现各业务模块独立打包发布。
技术栈：`React Native`、`TypeScript`、`@mrn/react-navigation`、`jotai`、`ahooks`

##### 核心工作

1. 采购域 4 个核心 `Bundle` 的融合重构，完成从 0 到 1 的目录结构搭建，包含路由配置、服务层封装、状态管理及页面组件拆分，确保各 `Bundle` 独立性
2. 基于公司内部基础库 `@sfe/wand-rn` 和 `@sgfe/flower-rn` 封装通用布局组件，通过 `HOC` 统一处理埋点、权限校验、业务配置等横切关注点
3. 建立 `ESLint` 规则 + `MR` 模板 + `CI` 卡口三层防护机制，拦截不安全数值运算，从流程上杜绝浮点精度隐患

#### 华为云 EI产品服务部 数据资产平台

##### 项目定位

作为华为云大模型数据支撑平台，为百亿参数训练提供多模态（文本/视频/结构化数据）的资产管理，
`Vue3` + `Vite` + `TypeScript` + `EChart5` + `ESLint` + `Husky` + `Lodash`

##### 核心工作

1. 开发支持PB级文本/视频/多模态数据的预览系统，实现视频智能分段，文本动态分页, 实现资产数据多维度检索
2. `ECharts5`开发平台数据资产可视化看板, 图表组件的扩展封装, `Three.js`实现动画特效
3. 数据处理任务管线搭建, 实现资产平台的前端导入、导出、更新功能, 任务处理的进度可视化以及进度详情的展示组件开发
4. 前端工程化实践, `Vite`打包压缩， 减小产物体积 **50%+**，提升流水线构建速度,
   `ESLint` + `Prettier`实现代码风格检验 + 自动格式化，拦截 **30%+** 的低级语法错误提前至开发阶段,
   `Husky` + `Commitlint` 实现前端CI/CD部署 + 自动化代码质量门禁
5. 探索`Tauri2.0`在项目中的落地，安装包体积较`Electron`缩减**65%**

#### 华为云 EI产品服务部 具身仿真平台

##### 项目定位

该项目是一个机械臂的仿真平台，用于选择、执行脚本，并查看，标定具身机械臂的任务执行情况, 技术栈`Vue3` + `NoVNC`

##### 核心工作

1. 团队前端工程化能力建设, 团队前端项目模板搭建，确保`Win` + `macOS`下有一致的开发体验
2. 搭建`VNC`传输管道, 服务器到前端的VNC传输管道并在前端展示, 优化NoVNC模块的带宽利用(18MBps→5MBps)，提高用户体验
3. 封装并沉淀 5+ 个通用业务组件（如脚本编辑器、状态监控面板），提升团队复用率。

#### 极氪 望朝大屏

该项目是一个服务于总裁办公室的大屏项目, 技术栈`Vue3` + `Echarts.GL`

实现了大屏项目从零到一的搭建，期间同步优化初期项目中存在性能问题以及组件的架构，

##### 工作内容

- 参与前端技术选型，需求评审等前期准备工作，撰写前端技术方案文档，方便后期项目维护
- 负责页面基础框架搭建和整体布局, 实现设计稿与大屏的适配
- 使用`Echarts.js`实现公司经营数据的图表可视化以及用户交互；`Echarts.GL`实现3D地球航线可视化的待机屏
- 实现前端数据本地缓存的封装，支持后端接口掉线时前端页面的正常显示
- `Vite`配置优化，方便项目的本地调试，优化不同环境时的接口请求逻辑
- 优化页面在多个图表同时请求数据以及加载时的性能，实现图表自动轮播时前后图表的懒加载，优化页面及网络性能
- 配置不同用户不同环境下的权限控制、页面展示内容控制等，实现前端的鉴权功能
- 优化项目研发效率，封装通用图表组件
- 使用`OSS`配置页面的接口轮询逻辑及部分数据的管理和展示
- 实现页面的无感刷新功能，优化用户体验

#### 极氪 极数团队 海外BI业务开发 2021.09 - 2022.03

该项目为公司BI业务的海外业务子项，主要用于查看海外业务的经营状况，使用公司内部的组件库开发, 技术栈 Vue3 + 无界

##### 工作内容

- 日常功能交互迭代
- 负责实现一个地图组件，利用原生`Vue3`实现地图的交互功能。通过点击实现地图自动聚焦和展示相关信息的tips，提升用户体验和数据展示效果。
- 参与开发部分前端公共组件的研发，负责日历组件部分交互逻辑的优化

## 个人项目

### PlayWrightPdfService(端到端 PDF 生成基础设施)

从零搭建了一套包含后端服务、前端 SDK、安全认证、容器化部署的 PDF 生成技术方案。
后端使用 .NET 10 + Playwright 渲染引擎，前端侧开发了 Nuxt 3 Module 与 Vue Composable，
生产部署上，服务运行在 Docker 容器中，通过 Cloudflare Zero Trust (Access + Tunnel) 实现零暴露面的公网访问，
同时内置 API Key 第二层认证兜底。服务已考虑国内网络环境，Dockerfile 使用阿里云镜像加速。

- [NPM仓库地址](https://www.npmjs.com/package/contribution-chart) [GitHub项目地址](https://github.com/cg37/contribution-chart)

### Mac信息验证码复制 (OTPilot)

一个轻量级 macOS 菜单栏应用，使用 Swift 原生开发，通过只读方式监控 chat.db 短信数据库，提取中英文验证码，复制到剪贴板并提醒用户。自动过滤手机号、订单号等干扰项，保留最近 50 条历史记录，已上架供个人使用，适用于依赖短信验证码登录的开发者与高频用户。

- [项目地址](https://github.com/cg37/OTPilot)

### Vision Bump

一款 VS Code 插件，在侧边栏提供一键式 npm 版本升级（patch/minor/major）并自动创建 Git 标签推送到远程仓库。采用 Vue 构建 Webview UI，操作直观，无需记忆命令，提升 Node.js 项目版本管理效率。

- [项目地址](https://marketplace.visualstudio.com/items?itemName=Craig37.version-bump)

### natural2bash (自然语言转 Bash 命令)

一个基于 AI 的命令行工具，将中文/英文自然语言描述自动转换为可执行的 `Bash` 命令
技术栈使用 `Node.js` 构建 `CLI` 框架，`Chalk` + `Ora` 优化终端视觉效果。内置 `GitHub Actions` 自动发布流水线：推送代码触发多版本 `Node.js（18.x / 20.x / 22.x）`矩阵测试的 `CI` 流水线，发布 `GitHub Release` 后自动触发 `npm` 公共仓库发布的 `CD` 流水线。

- [项目地址](https://github.com/cg37/natural2bash)

### BestDNS

一个基于 `Tauri v2` 开发的跨平台 DNS 测速桌面工具。前端使用 `React` + `TypeScript` + `Mantine UI`，后端采用 `Rust`实现 `DNS` 解析。支持一键测试 12 个国内外主流 DNS 服务器延迟，帮助用户快速选择最佳 `DNS`。支持 `Windows` 和 `macOS` 双平台，通过 `GitHub Actions` 实现自动构建与发布。

- [项目地址](https://github.com/cg37/BestDNS)

### FluxChat

FluxChat — 基于 Vue 3 的 ChatGPT 客户端。核心基于 SSE 协议实现流式输出，
通过 @microsoft/fetch-event-source 逐 token 解析 OpenAI 数据流，实时增量渲染至 UI，
达到打字机般逐字呈现效果。同时实现了自动滚动与用户交互暂停的智能联动：AI 输出期间话题区自动跟随滚动；
用户触摸或滚轮操作后，自动滚动暂停，避免打断阅读，交互恢复后重新跟随，整体提升了长文本对话的阅读连贯性和操作可控性。

- [项目地址](https://github.com/cg37/FluxChat#)

## 其他

5年+ 前端开发经验，深度参与多个大型企业级复杂系统(涵盖 B端管理、数据可视化大屏、移动端跨端)的核心模块研发与重构。
独立开发并发布了多款涵盖 NPM 包、VS Code 插件、CLI 工具及桌面端 (Tauri/Swift) 的提效产品，展现出极强的技术广度与产品闭环思维。
熟练搭建 CI/CD 自动化流水线，主导过前端代码规范门禁、通用组件库封装及构建体积优化，显著提升团队研发效能。
熟练阅读英文一手技术文档;积极拥抱 AI Agent 提效与 Spec Coding 规范，具备极强的技术落地与独立解决问题的能力。
